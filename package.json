{
  "name": "explorer",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "install": "cd client && npm install & cd server && npm install",
    "start": "cd client && npm start & cd server && npm start"
  }
}
